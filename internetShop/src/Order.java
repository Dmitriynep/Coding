public class Order {
    public void addProduct (Product product) {

//    ! Not a good way to copy product
//        Product copy = new Product(product.getName(), product.getPrice());
        //add copy to order
        //adds product
//        if (product.getPrice() > 50) {
//            product.setPrice(product.getPrice() - 25);
//        }
    }
}
